# Igual a
==  # Exemplo: a == b verifica se a é igual a b

# Diferente de
!=  # Exemplo: a != b verifica se a é diferente de b

# Maior que
>   # Exemplo: a > b verifica se a é maior que b

# Menor que
<   # Exemplo: a < b verifica se a é menor que b

# Maior ou igual a
>=  # Exemplo: a >= b verifica se a é maior ou igual a b

# Menor ou igual a
<=  # Exemplo: a <= b verifica se a é menor ou igual a b

# Adição
+   # Exemplo: a + b adiciona a e b

# Subtração
-   # Exemplo: a - b subtrai b de a

# Multiplicação
*   # Exemplo: a * b multiplica a por b

# Divisão
/   # Exemplo: a / b divide a por b

# Módulo (resto da divisão)
%   # Exemplo: a % b retorna o resto da divisão de a por b

# Exponenciação
**  # Exemplo: a ** b retorna a elevado à potência b

# Divisão inteira
//  # Exemplo: a // b retorna a parte inteira da divisão de a por b

# Atribuição
=   # Exemplo: a = 5 atribui o valor 5 à variável a

# Operadores de atribuição combinados com operações aritméticas
+=, -=, *=, /=, %=, **=, //=  # Exemplo: a += 5 é equivalente a a = a + 5

# Operador lógico "e"
and  # Exemplo: a and b retorna True se ambos a e b forem verdadeiros, senão retorna False

# Operador lógico "ou"
or   # Exemplo: a or b retorna True se pelo menos um dos a ou b forem verdadeiros, senão retorna False

# Operador lógico "não"
not  # Exemplo: not a retorna True se a for falso, senão retorna False

# Verifica se um valor está presente em uma sequência
in   # Exemplo: x in lista retorna True se x estiver na lista

# Verifica se dois objetos são o mesmo objeto na memória
is   # Exemplo: a is b retorna True se a é o mesmo objeto que b

# Verifica se um valor não está presente em uma sequência
not in  # Exemplo: x not in lista retorna True se x não estiver na lista

#formata em percentual sem nenhuma casa decimal
# :.0%